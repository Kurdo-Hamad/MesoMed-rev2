engine-strict=false
auto-install-peers=true
strict-peer-dependencies=false
# node-linker lives in pnpm-workspace.yaml (nodeLinker) as of pnpm 11 — a
# node-linker= line here is silently ignored (verified empirically in
# Phase 9a Slice 0, ADR-0018; this line stayed stale since Phase 0 while
# the workspace was actually isolated-linked).
