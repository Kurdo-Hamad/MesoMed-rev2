engine-strict=false
auto-install-peers=true
strict-peer-dependencies=false
# Metro's resolver assumes npm/yarn-style flat node_modules; Expo's own pnpm
# guide recommends hoisted linking to avoid missing-peer resolution errors
# in apps/mobile (see docs/adr/0001-locked-stack.md).
node-linker=hoisted
