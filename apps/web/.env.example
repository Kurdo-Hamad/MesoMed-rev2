# Base URL of the MesoMed API (no trailing slash).
NEXT_PUBLIC_API_URL=http://localhost:4000

# Origin serving facility/doctor media (next/image remotePatterns allowlist
# + CSP img-src). Defaults to the API origin until object storage is live.
NEXT_PUBLIC_MEDIA_URL=http://localhost:4000
