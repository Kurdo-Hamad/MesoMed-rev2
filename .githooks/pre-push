#!/bin/bash
while read -r local_ref local_sha remote_ref remote_sha; do
  if [ "$remote_ref" = "refs/heads/main" ]; then
    echo "ERROR: Direct push to main is blocked. Create a branch, open a PR, merge instead."
    exit 1
  fi
done
exit 0
