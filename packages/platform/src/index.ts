// Adapter interfaces (ai, search, storage, email, push, whatsapp, payments)
// are added one at a time as each concrete provider becomes real
// (MM-PLAN-001 §3.8 — never speculative). None exist yet in Phase 0.
export {};
